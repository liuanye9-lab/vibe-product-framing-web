# 2026-05-26

## 同步 GitHub 仓库

- 项目：vibe 优化版（VibePilot 产品思维训练 Web 应用）
- 目标仓库：`liuanye9-lab/vibe-product-framing-web`
- 操作：本地已有 git remote，发现分叉 commit，通过 rebase 解决，push 成功
- 远程已包含完整源码和配置

## Bug 修复 #1：JSON 解析失败（commit `087fecd`）

**问题**："模型返回格式错误，未找到有效 JSON"
**根因**：extractJson 贪婪正则对围栏/逗号/注释脆弱
**修复**：6 策略 JSON 修复管线 + prompt 强化
**范围**：4 文件，零 UI 变更

## Bug 修复 #2：Failed to fetch（commit `7b82d1e`）

**问题**："大模型生成失败：Failed to fetch"
**根因**：
1. fetch 无重试——一次网络抖动 = 失败
2. 错误信息空洞无诊断线索
3. vercel.json SPA catch-all 可能干扰 Edge Function 路由
4. 无连接预检

**修复**：
- 3 次重试 + 指数退避（仅可重试错误）
- classifyNetworkError() 6 类分类器 + 中文排错建议
- checkProxyReachable() OPTIONS 预检
- vercel.json 增加 /api/* 显式路由规则
- 错误消息含具体排查步骤
- 范围：evaluate.ts + vercel.json（+189/-45 行）

## Bug 修复 #3：模型输出截断导致 JSON 不完整（commit `46a5bd1`）

**问题**（用户 Vercel 线上报错）：
"模型返回格式错误，未找到有效 JSON。原始返回（前200字）：`{\"technical\":{\"architecture\":{\"value\":\"单页HTML+Tailwind...\",\"reason\":\"...\"},...},\"whyNoBackend\":{`"

**根因**：
1. **suggestStage 对所有阶段统一使用 maxTokens=850**，但 technical 阶段有 15+ 字段，远超预算
2. 模型输出到一半被 max_tokens 截断 → JSON 花括号不闭合 → parse 失败
3. 无截断检测/恢复机制——即使只差几个字符也无法挽救

**修复**：
- **STAGE_TOKEN_BUDGET 按阶段分配 token 预算**：discovery:900, product:1000, business:1200, **technical:2400**, mvp:1400, blindSpot:1300
- suggestIdeaDiagnosis: 850→2000（3 阶段合并输出）
- **新增 attemptTruncatedJsonRecovery()**：逐字符扫描器，自动补全未闭合的字符串/数组/对象括号
- **新增 detectTruncationHint()**：截断启发式检测，错误消息中报告具体未闭合数量
- tryRepairAndParse 管线从 6 策略扩展到 **8 策略**（新增 truncated recovery + balanced+truncated recovery）
- 范围：仅 evaluate.ts（+170/-3 行），零 UI 变更

## Bug 修复 #4：DeveloperHandoffPage React Error #31（commit `dea20e4`）

**问题**：页面显示"页面加载出错"，React Minified Error #31（Objects are not valid as a React child）

**根因**：DeveloperHandoffPage.tsx 多处将 handoff/evaluation/knowledgeReferences 的动态字段
直接作为 JSX child 渲染。localStorage 旧数据或异常 AI 输出中字段值可能为对象
（如 `{value: "xxx"}` 而非纯字符串），React 渲染时抛错。

**修复**：
- `src/pages/DeveloperHandoffPage.tsx`：
  - KnowledgeReferencesCard: title, type, score, reason, influence → toDisplayText
  - KeywordRow: map 循环 item → toDisplayText
  - EvaluationReportCard: readiness → toDisplayText
  - FixSuggestionsList: targetSection, id, issue, patch → toDisplayText
  - EvaluationDimensionCard: label, score → toDisplayText
  - EvaluationList: items → toDisplayList() 归一化
- `src/hooks/useProductBrief.ts`：saveFinalHandoff 写入前过 normalizeFinalHandoff
- import 增加 toDisplayList；构建 TC 零错误 + Vite build 通过

## Bug 修复 #5：assertAIOutputReferencesInput 单字段误报（commit `e4f1791`）

**问题**："AI 输出没有基于当前产品想法，请重新生成"

**根因**：`referencedCount < 2` 硬编码。用户只填 rawIdea（不填其他字段）时
fields 只有 1 条，永远达不到 2 → 100% 误报

**修复**：
- `minRequired = Math.min(2, fields.length)` — 1 字段时匹配 1 即通过
- 过滤 normalizeForReference 后为空的字段
- 范围：evaluate.ts（+13/-3 行）

## Vibe Copilot V1.6：AI Reliability Patch（commit `7a6b6c2`）

系统性修复 AI API 连接与输出校验的 6 个根因：
1. **代理超时**：api-proxy.ts + vite.config.ts 动态 timeoutMs (10000-240000ms)
2. **VibeAIError**：6 类错误 (connection/timeout/http/empty/json_parse/validation)
3. **加权引用校验**：replace hard throw → validateAIOutputReferencesInput()
4. **referenceEvidence**：prompt 增加顶层证明字段
5. **缓存 key**：加入 systemPrompt hash + clearAICache()
6. **Salvage**：valid JSON + weak ref → 警告而非丢弃

7. **Settings 长 JSON 测试**：3 层状态 (API / JSON Generation / Reference Validation)
8. **错误文案**：7 页统一 getAIErrorMessage() 分类展示
9. **API URL**：支持完整 endpoint + 非标提示

范围：17 files, +518/-90 lines

## Vibe Copilot V1.7：Fast MVP Decision Patch（commit `6094ad9`）

解决第二步 MVP Decision 频繁超时问题：

1. **Compact context**：新建 `src/prompts/stageContext.ts`，MVP 只用精简上下文
   （ideaInput + productSummary + demandSummary + businessSummary），≤180字/字段
2. **Lightweight prompt**：新建 `src/prompts/mvpScopePrompt.ts`，无技术规则/技术翻译
3. **Fast path**：mvp 用 900 tokens + 75000ms timeout，cache key `stage:mvp:fast`
4. **Pre-fill**：MvpScopePage 进入即填 `buildLocalMvpSuggestions()`，不自动调 AI
5. **Timeout fallback**：AI 超时返回 local-rule draft + 明确提示，不阻断流程
6. **Button label**：区分"AI 优化范围建议" vs "重新生成范围建议"

范围：5 files, +319/-14 lines

## Settings 长 JSON 测试误判修复（commit `2dee890`）

修复 Settings 长 JSON 测试误报"缺少关键字段"：

1. **Prompt 修正**：去掉 "..." 占位符 → 明确要求 ≥30中文字符 + 关键词
2. **JSON 解析复用**：extractJson 导出，Settings 不再手写 parse
3. **校验复用**：validateAIOutputReferencesInput 导出，4 层分层展示
4. **devSpec 校验**：原来只检查 productBrief/mvpScope/developmentPrompt
5. **isUsefulString**：自动拒绝 "..." / "N/A" / 空串
6. **LONG_TEST_BRIEF**：构造测试用 ProductBrief

范围：3 files, +193/-35 lines
